import { Environment } from './environments.interface';

export const environments = {
  baseUrl: 'http://localhost:3000', //Change url with production one
  production: true,
  showLogs: false
} as Environment;
